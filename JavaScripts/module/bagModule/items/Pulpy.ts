import { ItemBase } from "../ItemBase";
/**
 * 原素瓶
 */
export class Pulpy extends ItemBase {

    use(): void {
        // TODO: 喝原素瓶在PlayerModule写吗？
        // ModuleManager.instance.getModule(PlayerModuleC).
    }
}